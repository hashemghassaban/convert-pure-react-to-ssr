# convert-pure-react-to-ssr
Converting a pure React app to Server-Side Rendering (SSR) involves setting up a Node.js server (usually Express), using ReactDOMServer.renderToString to turn components into HTML on the server, and using ReactDOM.hydrateRoot on the client to add interactivity. 
